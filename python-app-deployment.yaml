apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: python-app
spec:
  replicas: 2
  minReadySeconds: 15
  strategy:
    type: RollingUpdate
    rollingUpdate: 
      maxUnavailable: 1
      maxSurge: 1 
  template:
    metadata:
      labels:
        app: python-app
    spec:
      containers:
        - image: 
          imagePullPolicy: Always
          name: sa-logic
          ports:
            - containerPort: 5000